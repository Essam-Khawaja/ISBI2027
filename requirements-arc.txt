# Linux ARC environment for the src baseline pipeline, Python 3.10+.
# Keep the local CUDA suffix so pip cannot select a CUDA 13 build.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.6.0+cu126
numpy==2.2.6
pandas==2.3.3
nibabel==5.3.2
tomli==2.2.1; python_version < "3.11"
